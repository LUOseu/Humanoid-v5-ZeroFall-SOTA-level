# Humanoid-v5-ZeroFall-SOTA-level
Humanoid-ZeroFall releases a high-return, low-variance CrossQ benchmark for Gymnasium Humanoid-v5. Our best run reaches ~9938 return with std ~30 over 10 deterministic evaluation episodes, with all episodes reaching the 1000-step horizon.our result on a stability-performance frontier rather than claiming highest raw return.
